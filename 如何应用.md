# Concrete 基线改版：k 通道选择器（ConcreteSelectorK）— 应用说明

基于当前 `main`（合并 PR #38 之后，`08a5da6`）制作，直接覆盖即可。

## 本次改了什么

把 Concrete 基线从"B 通道软掩码"改成忠实于 Balin et al. 2019（Appendix G
监督式 concrete selector）的 **k 通道选择器**，消除输入衰减伪影；topk 路径
（`DiagonalBandGate`）完全不动。

| 文件 | 改动 |
|---|---|
| `model/modules.py` | 删除 `ConcreteSelect`，替换为 `ConcreteSelectorK`：k 个选择节点，输出 k 通道 `z_j = Σ_b w_jb·x_b`（凸组合、保持幅值），指数退火 T0→TB；推理逐节点 argmax + 碰撞补齐 |
| `model/model.py` | `SegmentationModel` 新增 `encoder_in_channels` 参数；`build_model` 在 concrete 分支按 `k` 构建 encoder（topk 仍是 B）；`_build_band_gate` 返回新类 |
| `train.py` | 写 `selected_bands.json` 前先重载 `best_model.pth`（记录最佳模型的选择，而非最后一轮）；`best_model.pth` 里也存一份 `"selected_bands"` |
| `configs/concrete_k3.yaml` | `band_gate_tau_start: 10.0`（T0，原文热启动）/ `band_gate_tau_end: 0.01`（TB）、指数退火；`num_epochs: 500`、`early_stopping_patience: 70` |
| `scripts/run_gate_batch.py` | `--concrete` arm 自动覆盖基配置的 τ 为 T0=10.0/TB=0.01（否则会沿用 gate 的 cosine 1.0→0.05） |
| `README.md` | 基线小节按 k 通道语义重写 |

## 应用方式

方式一（覆盖文件）：把本包中 6 个文件按目录结构复制进仓库根目录覆盖，
提交推送。

方式二（git 补丁，保留提交信息）：

```bash
cd /path/to/msi
git am concrete-kchan.patch
git push
```

注意：补丁是基于 `08a5da6`（当前 main）的。如果 main 又有新提交且 `git am`
报冲突，就用方式一覆盖。

## 已验证（无需真实数据）

- 用 `configs/concrete_k3.yaml` 走 `build_model`：encoder 按 `in_channels=3`
  构建；`(2,15,64,64)` 输入 → 输出 `(2,2,64,64)`（train 和 eval 模式都过）。
- `get_selected_bands()` 返回 3 个不同波段；梯度流到 `alpha`。
- 指数退火：`set_gate_progress(0)` → T=10，`(0.5)` → 0.3162（=√(T0·TB)），
  `(1)` → 0.01。
- 幅值守恒：常数 1 输入 → 每个输出通道精确为 1（train 软权重按行归一，
  eval 硬 one-hot，均成立）。
- eval 模式输出通道 j 恰好等于（排序后）第 j 个被选波段的原始输入。
- `state_dict` 保存/加载往返一致（train.py 重载 best checkpoint 的路径）。
- topk 路径不受影响：`gate_k3` 构建 `DiagonalBandGate`，encoder 仍为 15
  输入，cosine τ 1.0→0.05 原样；无 gate 的 `baseline` 也正常。
- 批量脚本：concrete arm 拿到 T0=10/TB=0.01，gate arm 保持 1.0→0.05。

## 提醒

- concrete 分支下 `use_input_spectral_conv` / `use_spectral_conv` 必须为
  false（gate 已把通道从 B 变成 k，二者按 B 通道尺寸构建）——配置里本来就
  是关的，别开。
- 老的 concrete checkpoint（B 通道掩码版）与新代码不兼容（encoder 输入宽度
  从 B 变成了 k），需要重训。
- 仓库根目录里上次传上去的 `concrete-baseline.patch` 是补丁备份文件，
  建议顺手删掉。
